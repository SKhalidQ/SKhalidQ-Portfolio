import { DisableRightclickDirective } from './disable-rightclick.directive';

describe('DisableRightclickDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableRightclickDirective();
    expect(directive).toBeTruthy();
  });
});
