import { SocialButtonLogoPipe } from './social-button-logo.pipe';

describe('SocialButtonLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialButtonLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
