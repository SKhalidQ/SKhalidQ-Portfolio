import { GithubLogoPipe } from './github-logo.pipe';

describe('GithubLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new GithubLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
